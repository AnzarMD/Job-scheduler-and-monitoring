apiVersion: v1
kind: Service
metadata:
  name: cloudflow-service
  namespace: cloudflow
spec:
  selector:
    app: cloudflow-app
  ports:
    - port: 80
      targetPort: 8080
  type: NodePort          # Expose externally in Minikube